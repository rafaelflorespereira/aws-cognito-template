{
  "name": "aws-cognito-cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "bootstrap": "npx cdk bootstrap",
    "deploy": "npx cdk deploy --all --require-approval broadening",
    "destroy": "npx cdk destroy --all",
    "diff": "npx cdk diff",
    "synth": "npx cdk synth"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk": "^2.150.0",
    "typescript": "^5.4.0"
  }
}
