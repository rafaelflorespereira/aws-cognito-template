# Identifies the existing Cognito User Pool / App Client (see docs/deployment.md)
# that the sync API trusts to authorize requests. No new pool is created here.
CDK_DEFAULT_REGION=us-east-1
VS_USER_POOL_ID=<user_pool_id>
VS_USER_POOL_CLIENT_ID=<user_pool_client_id>
